---
import '../styles/almanac.css';

interface Props {
  title: string;
  section?: string;
  folioId?: string;
}

const { title, section = "Garden's Almanac", folioId = '' } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,500;0,8..60,600;0,8..60,700;1,8..60,400;1,8..60,500&family=IM+Fell+English+SC&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <article class="page">
      <header class="running-head">
        <span>Garden's Almanac of Matter Models</span>
        <span>{section}</span>
      </header>
      <slot />
      <footer class="folio">
        <span>{folioId}</span>
        <span class="num">·</span>
        <span>Garden's Almanac, 2026</span>
      </footer>
    </article>
  </body>
</html>
